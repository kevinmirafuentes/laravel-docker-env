services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: carrental-app
    container_name: carrental-app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - ./src:/var/www
    networks:
      - carrental-net
    depends_on:
      - mysql
    environment:
        MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-root}
        MYSQL_DATABASE: ${DB_DATABASE:-carrental}
        MYSQL_USER: ${DB_USERNAME:-root}
        MYSQL_PASSWORD: ${DB_PASSWORD:-root}
    expose:
      - 9000

  nginx:
    image: nginx:latest
    container_name: carrental-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./:/var/www
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - carrental-net

  mysql:
    image: mysql:8.4
    container_name: carrental-mysql
    restart: unless-stopped
    env_file:
      - ./src/.env
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - carrental-net

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: carrental-phpmyadmin
    restart: unless-stopped
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
    ports:
      - "8081:80"
    depends_on:
      - mysql
    networks:
      - carrental-net

  smtp4dev:
    image: rnwood/smtp4dev
    container_name: carrental-smtp4dev
    restart: unless-stopped
    ports:
      - "2525:25"
      - "3000:80"
    networks:
      - carrental-net

volumes:
  mysql-data:

networks:
  carrental-net:
    driver: bridge
